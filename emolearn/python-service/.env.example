# Python Service Environment Variables Template
# Copy this to .env and fill in your actual values
# DO NOT commit .env to GitHub

# Environment
ENVIRONMENT=production

# Backend API URL (where this service will make requests to)
BACKEND_API_URL=https://your-backend-api.railway.app

# Optional: API Key for backend authentication (if needed)
BACKEND_API_KEY=your-optional-api-key

# Optional: Port (usually set by Railway automatically)
PORT=8000
